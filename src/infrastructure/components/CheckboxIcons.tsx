import { CheckBox, CheckBoxOutlineBlank } from '@mui/icons-material'

export const CheckboxIcons = Object.freeze({
  checked: <CheckBox fontSize="small" />,
  unchecked: <CheckBoxOutlineBlank fontSize="small" />,
})
